interface:
  display_name: "TENeT"
  short_description: "Repo-specific patterns and workflows for TENeT"
  default_prompt: "Use the TENeT repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true